# Bot-specific dependencies
-r base.txt
aiohttp>=3.9.0
web3>=6.0.0
solders>=0.20.0
solana>=0.30.0
tenacity>=8.0.0
pyyaml>=6.0
aiosqlite>=0.19.0

# Privy for secure wallet infrastructure
privy>=0.1.0

# Keep these (still needed)
eth-account>=0.8.0
