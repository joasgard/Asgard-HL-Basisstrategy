# ============================================
# Delta Neutral Arb - Environment Configuration
# ============================================
#
# YOU HAVE TWO OPTIONS FOR CONFIGURING SECRETS:
#
# Option 1: Use this .env file (simple, good for local development)
#   - Copy this file to .env: cp .env.example .env
#   - Fill in your credentials below
#   - .env is git-ignored by default
#
# Option 2: Use the secrets/ directory (recommended for production)
#   - Copy example files: cp secrets/*.txt.example secrets/
#   - Rename and fill in credentials in secrets/ directory
#   - Files in secrets/ are git-ignored except .example templates
#   - See secrets/README.md for details
#
# Priority: Environment variables > Secret files > .env file values
# ============================================

# ============================================
# Asgard (Solana)
# ============================================
# Option A: Set directly in this file
# ASGARD_API_KEY=your_asgard_api_key_here
# SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your_helius_key
# SOLANA_PRIVATE_KEY=base58_encoded_private_key_or_path_to_keyfile

# Option B: Comment out above and use files in secrets/:
#   - secrets/asgard_api_key.txt
#   - secrets/solana_rpc_url.txt (optional, has default)
#   - secrets/solana_private_key.txt

# ============================================
# Hyperliquid (Arbitrum)
# ============================================
# Option A: Set directly in this file
# HYPERLIQUID_WALLET_ADDRESS=0x...
# HYPERLIQUID_PRIVATE_KEY=0x...  # secp256k1 - SEPARATE from Solana key
# ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/your_key  # Optional

# Option B: Comment out above and use files in secrets/:
#   - secrets/hyperliquid_wallet_address.txt
#   - secrets/hyperliquid_private_key.txt
#   - secrets/arbitrum_rpc_url.txt (optional)

# ============================================
# Admin / Operations
# ============================================
# ADMIN_API_KEY=secure_random_string_for_pause_resume
# Or use: secrets/admin_api_key.txt

# ============================================
# Environment
# ============================================
ENVIRONMENT=development  # development, staging, production
LOG_LEVEL=INFO           # DEBUG, INFO, WARNING, ERROR
PAPER_TRADING=true       # true = simulate trades, false = live trading

# ============================================
# Optional: Sentry for error tracking
# ============================================
# SENTRY_DSN=https://...@sentry.io/...
# Or use: secrets/sentry_dsn.txt

# ============================================
# Security Notes
# ============================================
# 1. Never commit this file with real credentials
# 2. Use the secrets/ directory for better isolation
# 3. Set restrictive permissions on credential files:
#    chmod 600 .env secrets/*.txt
# 4. Use separate keys for development and production
# 5. Consider hardware wallets for production keys
