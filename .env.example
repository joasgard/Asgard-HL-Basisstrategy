# =============================================================================
# Asgard Basis Environment Variables
# =============================================================================
# Copy to .env and fill in values: cp .env.example .env

# --- Infrastructure ---
DATABASE_URL=postgresql://basis:CHANGE_ME@localhost:5432/basis
POSTGRES_PASSWORD=CHANGE_ME
REDIS_URL=redis://localhost:6379

# --- Application ---
DASHBOARD_ENV=production
ALLOWED_ORIGINS=https://yourdomain.com
LOG_LEVEL=INFO
UVICORN_WORKERS=2

# --- Privy Authentication ---
VITE_PRIVY_APP_ID=your-privy-app-id
# Privy secrets go in secrets/ directory (see below)

# --- Frontend ---
VITE_API_BASE_URL=

# --- Blockchain RPCs ---
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
ARBITRUM_RPC_URL=https://arb1.arbitrum.io/rpc

# --- TLS / Docker ---
SSL_CERT_DIR=./certs
SECRETS_DIR=./secrets

# --- Secrets Directory ---
# The following 7 files must exist in the secrets/ directory:
#
#   asgard_api_key.txt       - Asgard API key (asgard.finance)
#   privy_app_id.txt         - Privy application ID (dashboard.privy.io)
#   privy_app_secret.txt     - Privy application secret
#   solana_rpc_url.txt       - Solana RPC URL (e.g., from helius.dev)
#   arbitrum_rpc_url.txt     - Arbitrum RPC URL (e.g., from alchemy.com)
#   server_secret.txt        - Generate with: openssl rand -hex 32
#   privy_auth.pem           - EC private key: openssl ecparam -name prime256v1 -genkey -noout -out privy_auth.pem
#
# Set permissions: chmod 600 secrets/*
