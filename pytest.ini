[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = . backend bot shared
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    critical: Critical safety tests
    integration: Integration tests requiring external services
    unit: Unit tests
asyncio_mode = auto
